/// Mobile stub — replaced by `google_web_auth_web.dart` on Flutter Web via
/// conditional imports. Throws on mobile because we use the native
/// `google_sign_in` plugin there.
Future<String?> signInWithGoogleWeb() async {
  throw UnsupportedError('signInWithGoogleWeb is web-only');
}
